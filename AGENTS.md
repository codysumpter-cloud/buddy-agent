# Buddy Agent

Buddy-agent is the target primary agent repository.

OpenClaw is retired for current work. Hermes-agent is the current main working agent system. Buddy-agent should become the primary and eventually only agent repository.
