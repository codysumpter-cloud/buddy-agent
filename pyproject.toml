[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "buddy-agent"
version = "0.1.0"
description = "Buddy-branded agent runtime scaffold for Prismtek."
readme = "README.md"
requires-python = ">=3.11"
authors = [{ name = "Cody Sumpter" }]
license = { file = "LICENSE" }
keywords = ["agent", "buddy", "prismtek", "runtime", "automation"]
classifiers = [
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
]
dependencies = []

[project.optional-dependencies]
dev = [
  "pytest>=8.0",
  "ruff>=0.8.0",
  "mypy>=1.10,<2.0",
]
mythos = [
  "torch>=2.3",
]

[project.scripts]
buddy = "buddy_agent.cli:main"
buddy-workspace = "buddy_agent.cli_workspace:main"
buddy-product-spine = "buddy_agent.cli_product_spine:main"
buddy-readiness = "buddy_agent.agent_readiness.cli:main"
buddy-trust = "buddy_agent.trust_fabric.cli:main"
buddy-mcp = "buddy_agent.mcp_runtime:main"
buddy-task = "buddy_agent.tasks.cli:main"
buddy-exec = "buddy_agent.execution.cli:main"
buddy-serve = "buddy_agent.http_server:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 100
src = ["src", "tests"]
extend-exclude = ["skills"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
"src/buddy_agent/control_plane/*.py" = ["ALL"]
"src/buddy_agent/mcp_server.py" = ["UP037"]
"tests/test_control_plane_runtime_adapter.py" = ["ALL"]

[tool.mypy]
python_version = "3.11"
strict = true
packages = ["buddy_agent"]
